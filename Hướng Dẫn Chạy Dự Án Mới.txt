Hướng Dẫn Chạy Dự Án 
dự án đã chia làm 2 phần, bạn sẽ cần chạy riêng biệt cả hai:

Chạy Backend (Cổng 5000 mặc định):

cd c:\Users\ADMIN\Documents\GitHub\QL_benh_vien\backend

node src/server.js


Chạy Frontend (Cổng Vite 5173 mặc định): Mở một cửa sổ Terminal MỚI:

cd frontend

npm run dev


có thể mở đường dẫn hiển thị trên Terminal của Vite (thường là http://localhost:5173) để xem giao diện trang Dashboard React + Tailwind mới nhất!